<h2 class="text-2xl font-bold mb-4">Upload Feedbacks (CSV)</h2>

<%= form_with url: upload_csv_feedbacks_path, method: :post, html: { multipart: true }, data: { turbo: false } do |form| %>
  <%= form.file_field :file, class: "..." %>
  <%= form.submit "Upload", class: "..." %>
<% end %>
